version: '3.8'

services:
  web-app:
    image: isdockero/onliver-web-v1:latest
    networks:
      - service-network
    deploy:
      replicas: 1
      restart_policy:
        condition: any


networks:
  service-network:
    external: true
